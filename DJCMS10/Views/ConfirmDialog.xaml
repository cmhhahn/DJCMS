<Window x:Class="DJCMS.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm" Height="170" Width="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Use application-level DarkAccentColor; provide brushes that reference it -->
        <SolidColorBrush x:Key="DialogAccentBrush" Color="{DynamicResource DarkAccentColor}" />
        <SolidColorBrush x:Key="DialogAccentHoverBrush" Color="{DynamicResource DarkAccentColor}" Opacity="0.88" />
        <SolidColorBrush x:Key="DialogAccentBorderBrush" Color="{DynamicResource DarkAccentColor}" Opacity="0.95" />

        <!-- Cancel button brushes -->
        <SolidColorBrush x:Key="DialogCancelBrush" Color="#2B2F33" />
        <SolidColorBrush x:Key="DialogCancelHoverBrush" Color="#34373C" />

        <!-- Button style for primary action -->
        <Style x:Key="PrimaryDialogButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DialogAccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource DialogAccentBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource DialogAccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button style for cancel action -->
        <Style x:Key="CancelDialogButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DialogCancelBrush}" />
            <Setter Property="Foreground" Value="#EEF6FA" />
            <Setter Property="BorderBrush" Value="#333" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource DialogCancelHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.95" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Padding="16" BorderBrush="#222" BorderThickness="1">
      <Border.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
          <GradientStop Color="#26282A" Offset="0"/>
          <GradientStop Color="#121213" Offset="1"/>
        </LinearGradientBrush>
      </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Confirm Delete" FontSize="20" Opacity="0.8" FontWeight="SemiBold" Foreground="#EEF6FA" />
            <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,8,0,0" Foreground="#B3B3B3">
                <Run Text="Are you sure you want to delete the playlist " />
                <Run Text="" x:Name="PlaylistNameRun" FontWeight="Bold" Foreground="#FFFFFF" />
                <Run Text=" from your playlist library?" />
            </TextBlock>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button x:Name="CancelButton" Width="90" Height="30" Margin="0,0,8,0" Content="Cancel" Style="{StaticResource CancelDialogButton}" Click="CancelButton_Click" />
                <Button x:Name="YesButton" Width="90" Height="30" Content="Delete" Style="{StaticResource PrimaryDialogButton}" Click="YesButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
